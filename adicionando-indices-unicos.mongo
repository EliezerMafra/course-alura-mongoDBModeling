
db.artigos.createIndex({slug: 1}, {unique: true}) // creates unique index

db.artigos.getIndexes() // to get the name of an index

db.artigos.dropIndex('slug_1') // deletes index given the name

db.artigos.deleteOne({
	slug: 'projeto-caelum-nas-faculdades'
})